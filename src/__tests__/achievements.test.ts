import { describe, it, expect } from "vitest"; import { getAll, unlock, unlocked } from "../services/achievements"; describe("achievements", () => { beforeEach(() => localStorage.clear()); it("has 10 achievements", () => { expect(getAll().length).toBe(10); }); it("unlocks achievement", () => { expect(unlock("first_game")).not.toBeNull(); }); it("returns null if already unlocked", () => { unlock("first_game"); expect(unlock("first_game")).toBeNull(); }); it("counts unlocked", () => { unlock("first_game"); unlock("score_100"); expect(unlocked()).toBe(2); }); });
