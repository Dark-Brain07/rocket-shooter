import{describe,it,expect}from'vitest';import{UserFlow}from'../analytics/UserFlow';
describe('UserFlow',()=>{it('tracks',()=>{const x=new UserFlow();x.track('click');expect(x.getMetrics().total).toBe(1);});it('flushes',()=>{const x=new UserFlow();x.track('a');x.flush();expect(x.getData()).toHaveLength(0);});});