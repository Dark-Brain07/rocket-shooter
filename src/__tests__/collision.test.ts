import { describe, it, expect } from "vitest"; import { rectsOverlap, circlesOverlap } from "../utils/collision"; describe("collision", () => { it("detects overlapping rects", () => { expect(rectsOverlap({x:0,y:0,width:10,height:10}, {x:5,y:5,width:10,height:10})).toBe(true); }); it("detects non-overlapping rects", () => { expect(rectsOverlap({x:0,y:0,width:10,height:10}, {x:20,y:20,width:10,height:10})).toBe(false); }); it("detects overlapping circles", () => { expect(circlesOverlap(0, 0, 5, 3, 0, 5)).toBe(true); }); it("detects non-overlapping circles", () => { expect(circlesOverlap(0, 0, 2, 10, 0, 2)).toBe(false); }); });
