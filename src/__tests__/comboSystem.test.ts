import { describe, it, expect } from "vitest"; import { ComboSystem } from "../systems/ComboSystem"; describe("ComboSystem", () => { it("increments on hit", () => { const c = new ComboSystem(); c.hit(); c.hit(); expect(c.get()).toBe(2); }); it("returns multiplier", () => { const c = new ComboSystem(); for (let i = 0; i < 5; i++) c.hit(); expect(c.multiplier()).toBe(2); }); it("resets", () => { const c = new ComboSystem(); c.hit(); c.reset(); expect(c.get()).toBe(0); }); });
