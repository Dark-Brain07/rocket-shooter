import { describe, it, expect } from "vitest"; import { getDaily } from "../services/dailyChallenge"; describe("daily challenge", () => { beforeEach(() => localStorage.clear()); it("returns challenge for today", () => { const c = getDaily(); expect(c.target).toBeGreaterThan(0); expect(c.done).toBe(false); }); it("is consistent same day", () => { expect(getDaily().target).toBe(getDaily().target); }); });
