import { describe, it, expect } from "vitest"; import { DifficultyScaler } from "../systems/DifficultyScaler"; describe("DifficultyScaler", () => { it("starts at base speed", () => { const d = new DifficultyScaler(3); expect(d.speed()).toBe(3); }); it("increases over time", () => { const d = new DifficultyScaler(3); d.update(30000); expect(d.speed()).toBeGreaterThan(3); }); it("tracks waves", () => { const d = new DifficultyScaler(); expect(d.wave()).toBe(1); d.update(45000); expect(d.wave()).toBe(2); }); });
