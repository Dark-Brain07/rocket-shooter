import { describe, it, expect } from "vitest"; import { ENEMY_TYPES, POWERUP_TYPES } from "../enemies"; describe("enemy types", () => { it("has four enemy types", () => { expect(Object.keys(ENEMY_TYPES)).toHaveLength(4); }); it("boss has highest health", () => { expect(ENEMY_TYPES.boss.health).toBeGreaterThan(ENEMY_TYPES.basic.health); }); it("boss gives most score", () => { expect(ENEMY_TYPES.boss.score).toBeGreaterThan(ENEMY_TYPES.fast.score); }); }); describe("powerup types", () => { it("has four powerup types", () => { expect(Object.keys(POWERUP_TYPES)).toHaveLength(4); }); it("each has duration", () => { Object.values(POWERUP_TYPES).forEach(p => expect(p.duration).toBeGreaterThan(0)); }); });
