import { describe, it, expect } from "vitest"; import { reportError, getReports, clearReports } from "../utils/errorReporter"; describe("errorReporter", () => { it("captures errors", () => { clearReports(); reportError(new Error("test")); expect(getReports()[0].msg).toBe("test"); }); it("clears", () => { reportError(new Error("x")); clearReports(); expect(getReports()).toEqual([]); }); });
