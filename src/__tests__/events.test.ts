import { describe, it, expect } from "vitest"; import { EventEmitter } from "../utils/events"; describe("EventEmitter", () => { it("emits to listeners", () => { const e = new EventEmitter(); let x = 0; e.on("inc", () => x++); e.emit("inc"); expect(x).toBe(1); }); it("removes listeners", () => { const e = new EventEmitter(); let x = 0; const fn = () => x++; e.on("e", fn); e.off("e", fn); e.emit("e"); expect(x).toBe(0); }); it("clears all", () => { const e = new EventEmitter(); e.on("a", () => {}); e.clear(); }); });
