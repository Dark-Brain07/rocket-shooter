import { describe, it, expect } from "vitest"; import { getFlags, setFlag, isEnabled, resetFlags } from "../utils/featureFlags"; describe("feature flags", () => { beforeEach(() => resetFlags()); it("has defaults", () => { expect(getFlags().enableSound).toBe(true); }); it("toggles flags", () => { setFlag("showFPS", true); expect(isEnabled("showFPS")).toBe(true); }); it("resets", () => { setFlag("showFPS", true); resetFlags(); expect(isEnabled("showFPS")).toBe(false); }); });
