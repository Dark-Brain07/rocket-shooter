import { describe, it, expect } from "vitest"; import { truncateAddress, formatSTX, formatScore } from "../utils/format"; describe("truncateAddress", () => { it("truncates long addresses", () => { expect(truncateAddress("SP1234567890ABCDEF1234567890ABCDEF12345678")).toContain("..."); }); it("returns empty for empty input", () => { expect(truncateAddress("")).toBe(""); }); }); describe("formatSTX", () => { it("converts micro to STX", () => { expect(formatSTX(1000000)).toBe("1"); }); }); describe("formatScore", () => { it("formats thousands", () => { expect(formatScore(1500)).toBe("1.5K"); }); });
