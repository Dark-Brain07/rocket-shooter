import{describe,it,expect}from"vitest";import{CraftAnim}from"../../game/crafting/CraftAnim";
describe("CraftAnim",()=>{it("ok",()=>expect(new CraftAnim()).toBeDefined());it("toggle",()=>{const x=new CraftAnim();x.disable();expect(x.isActive()).toBe(false);});it("data",()=>{const x=new CraftAnim();x.set("a",1);expect(x.get("a")).toBe(1);});});