import{describe,it,expect}from"vitest";import{CraftFav}from"../../game/crafting/CraftFav";
describe("CraftFav",()=>{it("ok",()=>expect(new CraftFav()).toBeDefined());it("toggle",()=>{const x=new CraftFav();x.disable();expect(x.isActive()).toBe(false);});it("data",()=>{const x=new CraftFav();x.set("a",1);expect(x.get("a")).toBe(1);});});