import{describe,it,expect}from"vitest";import{CraftResult}from"../../game/crafting/CraftResult";
describe("CraftResult",()=>{it("ok",()=>expect(new CraftResult()).toBeDefined());it("toggle",()=>{const x=new CraftResult();x.disable();expect(x.isActive()).toBe(false);});it("data",()=>{const x=new CraftResult();x.set("a",1);expect(x.get("a")).toBe(1);});});