import{describe,it,expect}from"vitest";import{CraftSlot}from"../../game/crafting/CraftSlot";
describe("CraftSlot",()=>{it("ok",()=>expect(new CraftSlot()).toBeDefined());it("toggle",()=>{const x=new CraftSlot();x.disable();expect(x.isActive()).toBe(false);});it("data",()=>{const x=new CraftSlot();x.set("a",1);expect(x.get("a")).toBe(1);});});