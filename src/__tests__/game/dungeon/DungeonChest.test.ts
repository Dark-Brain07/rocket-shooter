import{describe,it,expect}from"vitest";import{DungeonChest}from"../../game/dungeon/DungeonChest";
describe("DungeonChest",()=>{it("ok",()=>expect(new DungeonChest()).toBeDefined());it("toggle",()=>{const x=new DungeonChest();x.disable();expect(x.isActive()).toBe(false);});it("data",()=>{const x=new DungeonChest();x.set("a",1);expect(x.get("a")).toBe(1);});});