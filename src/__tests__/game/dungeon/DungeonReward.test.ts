import{describe,it,expect}from"vitest";import{DungeonReward}from"../../game/dungeon/DungeonReward";
describe("DungeonReward",()=>{it("ok",()=>expect(new DungeonReward()).toBeDefined());it("toggle",()=>{const x=new DungeonReward();x.disable();expect(x.isActive()).toBe(false);});it("data",()=>{const x=new DungeonReward();x.set("a",1);expect(x.get("a")).toBe(1);});});