import{describe,it,expect}from"vitest";import{TradeWindow}from"../../game/economy/TradeWindow";
describe("TradeWindow",()=>{it("ok",()=>expect(new TradeWindow()).toBeDefined());it("toggle",()=>{const x=new TradeWindow();x.disable();expect(x.isActive()).toBe(false);});it("data",()=>{const x=new TradeWindow();x.set("a",1);expect(x.get("a")).toBe(1);});});