import{describe,it,expect}from"vitest";import{PetAbility}from"../../game/pets/PetAbility";
describe("PetAbility",()=>{it("ok",()=>expect(new PetAbility()).toBeDefined());it("toggle",()=>{const x=new PetAbility();x.disable();expect(x.isActive()).toBe(false);});it("data",()=>{const x=new PetAbility();x.set("a",1);expect(x.get("a")).toBe(1);});});