import{describe,it,expect}from"vitest";import{PetDisplay}from"../../game/pets/PetDisplay";
describe("PetDisplay",()=>{it("ok",()=>expect(new PetDisplay()).toBeDefined());it("toggle",()=>{const x=new PetDisplay();x.disable();expect(x.isActive()).toBe(false);});it("data",()=>{const x=new PetDisplay();x.set("a",1);expect(x.get("a")).toBe(1);});});