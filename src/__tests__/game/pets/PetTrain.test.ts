import{describe,it,expect}from"vitest";import{PetTrain}from"../../game/pets/PetTrain";
describe("PetTrain",()=>{it("ok",()=>expect(new PetTrain()).toBeDefined());it("toggle",()=>{const x=new PetTrain();x.disable();expect(x.isActive()).toBe(false);});it("data",()=>{const x=new PetTrain();x.set("a",1);expect(x.get("a")).toBe(1);});});