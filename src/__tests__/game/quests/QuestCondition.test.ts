import{describe,it,expect}from"vitest";import{QuestCondition}from"../../game/quests/QuestCondition";
describe("QuestCondition",()=>{it("ok",()=>expect(new QuestCondition()).toBeDefined());it("toggle",()=>{const x=new QuestCondition();x.disable();expect(x.isActive()).toBe(false);});it("data",()=>{const x=new QuestCondition();x.set("a",1);expect(x.get("a")).toBe(1);});});