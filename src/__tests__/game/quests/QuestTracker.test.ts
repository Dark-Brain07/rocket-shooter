import{describe,it,expect}from"vitest";import{QuestTracker}from"../../game/quests/QuestTracker";
describe("QuestTracker",()=>{it("ok",()=>expect(new QuestTracker()).toBeDefined());it("toggle",()=>{const x=new QuestTracker();x.disable();expect(x.isActive()).toBe(false);});it("data",()=>{const x=new QuestTracker();x.set("a",1);expect(x.get("a")).toBe(1);});});