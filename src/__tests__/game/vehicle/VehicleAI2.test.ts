import{describe,it,expect}from"vitest";import{VehicleAI2}from"../../game/vehicle/VehicleAI2";
describe("VehicleAI2",()=>{it("ok",()=>expect(new VehicleAI2()).toBeDefined());it("toggle",()=>{const x=new VehicleAI2();x.disable();expect(x.isActive()).toBe(false);});it("data",()=>{const x=new VehicleAI2();x.set("a",1);expect(x.get("a")).toBe(1);});});