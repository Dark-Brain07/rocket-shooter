import{describe,it,expect}from"vitest";import{SnowEffect}from"../../game/weather/SnowEffect";
describe("SnowEffect",()=>{it("ok",()=>expect(new SnowEffect()).toBeDefined());it("toggle",()=>{const x=new SnowEffect();x.disable();expect(x.isActive()).toBe(false);});it("data",()=>{const x=new SnowEffect();x.set("a",1);expect(x.get("a")).toBe(1);});});