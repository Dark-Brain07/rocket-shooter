import { describe, it, expect } from "vitest"; import { t, setLocale, getLocale, getLocales } from "../i18n"; describe("i18n", () => { it("translates English", () => { setLocale("en"); expect(t("menu.play")).toBe("Start Game"); }); it("translates Spanish", () => { setLocale("es"); expect(t("menu.play")).toBe("Iniciar Juego"); setLocale("en"); }); it("falls back to key", () => { expect(t("x.y.z")).toBe("x.y.z"); }); it("lists locales", () => { expect(getLocales()).toContain("en"); }); });
