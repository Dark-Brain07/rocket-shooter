import { describe, it, expect } from "vitest"; import { getBoard, addScore, getRank, clearBoard } from "../services/leaderboard"; describe("leaderboard", () => { beforeEach(() => { localStorage.clear(); clearBoard(); }); it("starts empty", () => { expect(getBoard()).toEqual([]); }); it("adds sorted entries", () => { addScore("SP1", 100); addScore("SP2", 500); expect(getBoard()[0].score).toBe(500); }); it("returns rank", () => { addScore("SP1", 100); addScore("SP2", 500); expect(getRank("SP2")).toBe(1); }); it("caps at 50", () => { for (let i = 0; i < 60; i++) addScore("SP" + i, i); expect(getBoard().length).toBe(50); }); });
