import { describe, it, expect } from "vitest"; import { clamp, lerp, randomInt, distance } from "../utils/math"; describe("math utils", () => { it("clamps values", () => { expect(clamp(-5, 0, 10)).toBe(0); expect(clamp(15, 0, 10)).toBe(10); expect(clamp(5, 0, 10)).toBe(5); }); it("interpolates linearly", () => { expect(lerp(0, 10, 0.5)).toBe(5); }); it("generates random integers", () => { const r = randomInt(1, 10); expect(r).toBeGreaterThanOrEqual(1); expect(r).toBeLessThanOrEqual(10); }); it("calculates distance", () => { expect(distance(0, 0, 3, 4)).toBe(5); }); });
