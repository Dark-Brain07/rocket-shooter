import { describe, it, expect } from "vitest"; import { createParticle, updateParticle } from "../utils/particles"; describe("particles", () => { it("creates particle at position", () => { const p = createParticle(10, 20, "#fff"); expect(p.x).toBe(10); expect(p.y).toBe(20); }); it("updates particle position", () => { const p = createParticle(0, 0, "#fff"); const alive = updateParticle(p); expect(alive).toBe(true); expect(p.life).toBe(2); }); });
