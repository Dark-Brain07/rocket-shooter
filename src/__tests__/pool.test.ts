import { describe, it, expect } from "vitest"; import { ObjectPool } from "../utils/pool"; describe("ObjectPool", () => { it("creates objects from factory", () => { const pool = new ObjectPool(() => ({ x: 0, y: 0 }), 5); expect(pool.size).toBe(5); }); it("returns and releases objects", () => { const pool = new ObjectPool(() => ({ x: 0 }), 2); const obj = pool.get(); pool.release(obj); expect(pool.size).toBe(2); }); });
