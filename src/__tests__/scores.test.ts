import { describe, it, expect } from "vitest"; import { getHighScore, setHighScore } from "../utils/scores"; describe("scores", () => { beforeEach(() => localStorage.clear()); it("returns 0 when empty", () => { expect(getHighScore()).toBe(0); }); it("updates when higher", () => { expect(setHighScore(100)).toBe(true); expect(getHighScore()).toBe(100); }); it("ignores lower scores", () => { setHighScore(100); expect(setHighScore(50)).toBe(false); }); });
