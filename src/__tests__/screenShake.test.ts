import { describe, it, expect } from "vitest"; import { ScreenShake } from "../systems/ScreenShake"; describe("ScreenShake", () => { it("starts inactive", () => { const s = new ScreenShake(); expect(s.active()).toBe(false); }); it("activates on trigger", () => { const s = new ScreenShake(); s.trigger(10, 500); expect(s.active()).toBe(true); }); it("decays", () => { const s = new ScreenShake(); s.trigger(10, 100); s.update(100); expect(s.active()).toBe(false); }); });
