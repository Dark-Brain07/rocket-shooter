import { describe, it, expect } from "vitest"; import { sanitizeInput, isValidUrl, generateNonce } from "../utils/security/sanitize"; describe("security", () => { it("escapes HTML", () => { expect(sanitizeInput("<b>hi</b>")).not.toContain("<b>"); }); it("validates URLs", () => { expect(isValidUrl("https://x.com")).toBe(true); expect(isValidUrl("nope")).toBe(false); }); it("generates unique nonces", () => { expect(generateNonce()).not.toBe(generateNonce()); }); });
