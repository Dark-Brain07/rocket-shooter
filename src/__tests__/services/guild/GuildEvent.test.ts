import{describe,it,expect}from"vitest";import{GuildEvent}from"../../services/guild/GuildEvent";
describe("GuildEvent",()=>{it("ok",()=>expect(new GuildEvent()).toBeDefined());it("toggle",()=>{const x=new GuildEvent();x.disable();expect(x.isActive()).toBe(false);});it("data",()=>{const x=new GuildEvent();x.set("a",1);expect(x.get("a")).toBe(1);});});