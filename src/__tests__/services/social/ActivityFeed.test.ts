import{describe,it,expect}from"vitest";import{ActivityFeed}from"../../services/social/ActivityFeed";
describe("ActivityFeed",()=>{it("ok",()=>expect(new ActivityFeed()).toBeDefined());it("toggle",()=>{const x=new ActivityFeed();x.disable();expect(x.isActive()).toBe(false);});it("data",()=>{const x=new ActivityFeed();x.set("a",1);expect(x.get("a")).toBe(1);});});