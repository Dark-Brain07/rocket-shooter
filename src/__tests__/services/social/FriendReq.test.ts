import{describe,it,expect}from"vitest";import{FriendReq}from"../../services/social/FriendReq";
describe("FriendReq",()=>{it("ok",()=>expect(new FriendReq()).toBeDefined());it("toggle",()=>{const x=new FriendReq();x.disable();expect(x.isActive()).toBe(false);});it("data",()=>{const x=new FriendReq();x.set("a",1);expect(x.get("a")).toBe(1);});});