import{describe,it,expect}from"vitest";import{ProfileCard}from"../../services/social/ProfileCard";
describe("ProfileCard",()=>{it("ok",()=>expect(new ProfileCard()).toBeDefined());it("toggle",()=>{const x=new ProfileCard();x.disable();expect(x.isActive()).toBe(false);});it("data",()=>{const x=new ProfileCard();x.set("a",1);expect(x.get("a")).toBe(1);});});