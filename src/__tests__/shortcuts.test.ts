import { describe, it, expect, vi } from 'vitest'; import { ShortcutManager } from '../utils/shortcuts';
describe('ShortcutManager', () => { it('fires handler', () => { const sm = new ShortcutManager(); const fn = vi.fn(); sm.register({ key: 'p', description: 'Pause', handler: fn }); sm.handleKeyDown(new KeyboardEvent('keydown', { key: 'p' })); expect(fn).toHaveBeenCalled(); }); it('disabled ignores', () => { const sm = new ShortcutManager(); const fn = vi.fn(); sm.register({ key: 'p', description: 'P', handler: fn }); sm.disable(); sm.handleKeyDown(new KeyboardEvent('keydown', { key: 'p' })); expect(fn).not.toHaveBeenCalled(); }); });