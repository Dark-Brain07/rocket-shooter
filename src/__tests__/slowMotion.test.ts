import { describe, it, expect } from "vitest"; import { SlowMotion } from "../systems/SlowMotion"; describe("SlowMotion", () => { it("starts inactive", () => { const s = new SlowMotion(); expect(s.active()).toBe(false); }); it("returns reduced scale", () => { const s = new SlowMotion(); s.trigger(0.3, 1000); expect(s.update(16)).toBe(0.3); }); it("ends after duration", () => { const s = new SlowMotion(); s.trigger(0.3, 100); s.update(100); expect(s.active()).toBe(false); }); });
