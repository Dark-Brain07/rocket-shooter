import { describe, it, expect } from "vitest"; import { getStorageItem, setStorageItem, removeStorageItem } from "../utils/storage"; describe("storage", () => { beforeEach(() => localStorage.clear()); it("returns fallback for missing key", () => { expect(getStorageItem("nope", 42)).toBe(42); }); it("stores and retrieves", () => { setStorageItem("k", { a: 1 }); expect(getStorageItem("k", null)).toEqual({ a: 1 }); }); it("removes items", () => { setStorageItem("k", "v"); removeStorageItem("k"); expect(getStorageItem("k", null)).toBeNull(); }); });
