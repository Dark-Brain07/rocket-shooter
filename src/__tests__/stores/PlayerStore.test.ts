import{describe,it,expect}from'vitest';import{PlayerStore}from'../stores/PlayerStore';
describe('PlayerStore',()=>{it('stores data',()=>{const x=new PlayerStore();x.set('a',1);expect(x.get('a')).toBe(1);});});