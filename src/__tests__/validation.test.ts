import { describe, it, expect } from "vitest"; import { isValidStacksAddress, isValidTxId, isValidScore } from "../utils/validation"; describe("validation", () => { it("accepts valid address", () => { expect(isValidStacksAddress("SP1YH5MXTJT86BZXMFA2T51JF0QVZ8XNYV33QH6MF")).toBe(true); }); it("rejects empty address", () => { expect(isValidStacksAddress("")).toBe(false); }); it("validates tx id format", () => { expect(isValidTxId("0x" + "a".repeat(64))).toBe(true); }); it("validates score range", () => { expect(isValidScore(500)).toBe(true); expect(isValidScore(-1)).toBe(false); }); });
