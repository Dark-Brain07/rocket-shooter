import { describe, it, expect } from "vitest"; import { WAVES } from "../waves"; describe("waves", () => { it("has multiple waves", () => { expect(WAVES.length).toBeGreaterThanOrEqual(5); }); it("waves have enemies", () => { WAVES.forEach(w => expect(w.enemies.length).toBeGreaterThan(0)); }); it("later waves are harder", () => { const first = WAVES[0].enemies.reduce((s, e) => s + e.count, 0); const last = WAVES[WAVES.length - 1].enemies.reduce((s, e) => s + e.count, 0); expect(last).toBeGreaterThan(first); }); });
