import { describe, it, expect } from "vitest"; import { WEAPONS } from "../systems/WeaponSystem"; describe("weapons", () => { it("has four types", () => { expect(Object.keys(WEAPONS)).toHaveLength(4); }); it("heavy does most damage", () => { expect(WEAPONS.heavy.damage).toBeGreaterThan(WEAPONS.basic.damage); }); it("rapid fires fastest", () => { expect(WEAPONS.rapid.fireRate).toBeLessThan(WEAPONS.basic.fireRate); }); it("each has color", () => { Object.values(WEAPONS).forEach(w => expect(w.color).toBeTruthy()); }); });
