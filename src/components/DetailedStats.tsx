import React from 'react'; import { getStats } from '../services/statistics';
export const DetailedStats: React.FC = () => { const s = getStats(); const items = [{l:'Games',v:s.totalGames},{l:'Best Score',v:s.bestScore},{l:'Total Kills',v:s.totalKills},{l:'Highest Wave',v:s.highestWave},{l:'Avg Score',v:s.avgScore}]; return React.createElement('div', { className: 'grid grid-cols-2 gap-3' }, items.map(i => React.createElement('div', { key: i.l, className: 'bg-gray-800/50 rounded-lg p-3' }, React.createElement('div', { className: 'text-xs text-gray-400' }, i.l), React.createElement('div', { className: 'text-lg font-bold text-white' }, i.v)))); };