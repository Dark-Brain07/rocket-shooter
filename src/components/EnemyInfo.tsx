import React from 'react'; import { ENEMY_TYPES } from '../game/enemies/EnemyTypes';
export const EnemyInfo: React.FC<{ typeId: string }> = ({ typeId }) => { const e = ENEMY_TYPES[typeId]; if (!e) return null; return React.createElement('div', { className: 'flex items-center gap-2 p-2 bg-gray-800/50 rounded' }, React.createElement('div', { className: 'w-4 h-4 rounded', style: { backgroundColor: e.color } }), React.createElement('div', null, React.createElement('div', { className: 'text-xs font-bold text-white' }, e.name), React.createElement('div', { className: 'text-xs text-gray-400' }, 'HP:' + e.hp + ' SPD:' + e.speed))); };