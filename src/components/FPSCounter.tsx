import React, { useEffect, useState } from "react"; export const FPSCounter: React.FC = () => { const [fps, setFps] = useState(0); useEffect(() => { let frames = 0, last = performance.now(); const loop = () => { frames++; const now = performance.now(); if (now - last >= 1000) { setFps(frames); frames = 0; last = now; } requestAnimationFrame(loop); }; const id = requestAnimationFrame(loop); return () => cancelAnimationFrame(id); }, []); return React.createElement("div", { className: "fixed top-2 right-2 text-xs font-mono text-green-400 bg-black/50 px-2 py-1 rounded" }, fps + " FPS"); };
