import React from "react"; export const HealthBar: React.FC<{ current: number; max: number }> = ({ current, max }) => { const pct = Math.max(0, (current / max) * 100); const color = pct > 60 ? "bg-green-500" : pct > 30 ? "bg-yellow-500" : "bg-red-500"; return React.createElement("div", { className: "w-32 h-3 bg-gray-700 rounded-full overflow-hidden" }, React.createElement("div", { className: "h-full rounded-full transition-all duration-300 " + color, style: { width: pct + "%" } })); };
