import React from "react"; interface ModalProps { open: boolean; onClose: () => void; title?: string; children: React.ReactNode; } export const Modal: React.FC<ModalProps> = ({ open, onClose, title, children }) => { if (!open) return null; return React.createElement("div", { className: "fixed inset-0 z-50 flex items-center justify-center bg-black/70 backdrop-blur-sm", onClick: onClose }, React.createElement("div", { className: "bg-gray-800 border border-gray-700 rounded-xl p-6 max-w-md w-full mx-4", onClick: (e: any) => e.stopPropagation() }, title && React.createElement("h2", { className: "text-xl font-bold text-white mb-4" }, title), children)); };
