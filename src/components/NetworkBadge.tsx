import React from "react"; export const NetworkBadge: React.FC<{ network: "mainnet" | "testnet" }> = ({ network }) => { const m = network === "mainnet"; return React.createElement("div", { className: "flex items-center gap-1.5 text-xs" }, React.createElement("div", { className: "w-2 h-2 rounded-full animate-pulse " + (m ? "bg-green-400" : "bg-yellow-400") }), React.createElement("span", { className: m ? "text-green-400" : "text-yellow-400" }, m ? "Mainnet" : "Testnet")); };
