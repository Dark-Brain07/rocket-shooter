import React from 'react'; import { POWERUPS } from '../game/powerups/definitions';
export const PowerUpBar: React.FC<{ activeIds: string[] }> = ({ activeIds }) => { if (!activeIds.length) return null; return React.createElement('div', { className: 'flex gap-2 p-2' }, activeIds.map(id => { const p = POWERUPS.find(x => x.id === id); if (!p) return null; return React.createElement('div', { key: id, className: 'flex items-center gap-1 px-2 py-1 rounded-full text-xs font-bold text-white', style: { backgroundColor: p.color + '40', border: '1px solid ' + p.color } }, React.createElement('span', null, p.icon), React.createElement('span', null, p.name)); })); };