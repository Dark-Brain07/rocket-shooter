import React from "react"; import { formatScore } from "../utils/format"; export const ScoreDisplay: React.FC<{ score: number; highScore: number }> = ({ score, highScore }) => { const isHigh = score > 0 && score >= highScore; return React.createElement("div", { className: "text-center" }, React.createElement("div", { className: "text-sm text-gray-400" }, "SCORE"), React.createElement("div", { className: "text-3xl font-bold " + (isHigh ? "text-yellow-400" : "text-white") }, formatScore(score)), highScore > 0 && React.createElement("div", { className: "text-xs text-gray-500" }, "Best: " + formatScore(highScore))); };
