import React, { useState, useEffect } from "react"; export const Toast: React.FC<{ message: string; type?: "success" | "error" | "info"; onClose: () => void }> = ({ message, type = "info", onClose }) => { const [visible, setVisible] = useState(true); useEffect(() => { const t = setTimeout(() => { setVisible(false); setTimeout(onClose, 300); }, 3000); return () => clearTimeout(t); }, [onClose]); const c = { success: "bg-green-800 border-green-600", error: "bg-red-800 border-red-600", info: "bg-blue-800 border-blue-600" }; return React.createElement("div", { className: "fixed bottom-4 right-4 z-50 px-4 py-3 rounded-lg border text-white transition-all " + c[type] + (visible ? " opacity-100" : " opacity-0") }, message); };
