interface Flags { enableLeaderboard: boolean; enablePowerUps: boolean; enableSound: boolean; showFPS: boolean; } const defaults: Flags = { enableLeaderboard: false, enablePowerUps: false, enableSound: true, showFPS: false }; let current = { ...defaults }; export function getFlags(): Flags { return { ...current }; } export function setFlag<K extends keyof Flags>(k: K, v: Flags[K]): void { current[k] = v; } export function isEnabled(k: keyof Flags): boolean { return current[k]; } export function resetFlags(): void { current = { ...defaults }; }
