export class Timer{private remaining:number;private running=false;constructor(private duration:number){this.remaining=duration;}start(){this.running=true;this.remaining=this.duration;}stop(){this.running=false;}update(dt:number){if(this.running){this.remaining-=dt;if(this.remaining<=0){this.running=false;this.remaining=0;}}}isDone():boolean{return this.remaining<=0;}getProgress():number{return 1-this.remaining/this.duration;}reset(){this.remaining=this.duration;this.running=false;}}